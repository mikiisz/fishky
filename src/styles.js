import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        padding: 20
    },
    headerText: {
        fontSize: 35,
        fontWeight: "bold",
        textAlign: "center"
    },
    contentText: {
        fontSize: 20,
        marginTop: 20
    }
})

export {styles}